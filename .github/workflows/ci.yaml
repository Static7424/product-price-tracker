name: CI

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    tags-ignore:
      - "v*"
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    environment: test
    env:
      ENVIRONMENT: ${{ vars.ENVIRONMENT }}
      POSTGRES_VERSION: ${{ vars.POSTGRES_VERSION }}
      POSTGRES_PORT: ${{ vars.POSTGRES_PORT }}
      POSTGRES_DATABASE: ${{ vars.POSTGRES_DATABASE }}
      POSTGRES_USER: ${{ vars.POSTGRES_USER }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      POSTGRES_URL: ${{ vars.POSTGRES_URL }}
    services:
      postgres:
        image: postgres:${{ vars.POSTGRES_VERSION }}-alpine
        env:
          POSTGRES_DB: ${{ vars.POSTGRES_DATABASE }}
          POSTGRES_USER: ${{ vars.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
        ports:
          - "${{ vars.POSTGRES_PORT }}:5432"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            pg_isready -h localhost -U ${{ env.POSTGRES_USER }} -d ${{ env.POSTGRES_DATABASE }} && break
            sleep 1
          done

      - name: Initialize Database
        run: PGPASSWORD=${{ env.POSTGRES_PASSWORD }} psql -h localhost -U ${{ env.POSTGRES_USER }} -d ${{ env.POSTGRES_DATABASE }} -f database/init_db.sql

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.6.1"

      - name: Lint (Ruff)
        run: uv run ruff check .

      - name: Format Check
        run: uv run ruff format --check .

      - name: Run Tests
        run: uv run pytest

      - name: Upload Test Coverage
        uses: codecov/codecov-action@v5
        with:
          files: coverage.xml

      - name: Security Scan (Bandit)
        run: uv run bandit -r src/

      - name: Dependency Vulnerability Check
        run: uv run pip-audit
  
  docker:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Docker Build Verification
        run: docker build -f docker/Dockerfile .
